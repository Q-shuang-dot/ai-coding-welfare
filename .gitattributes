# 统一换行符：脚本跨平台可执行，避免 Windows 下 CRLF 污染 bash 脚本
* text=auto eol=lf
*.sh text eol=lf
*.ps1 text eol=crlf
# cmd.exe 解析 LF-only 批处理会丢字符（尤其带 ^ 续行和中文注释时），必须 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
*.png binary
*.jpg binary
*.ico binary
