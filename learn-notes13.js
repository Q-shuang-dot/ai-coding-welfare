/* ============ 追加批次 14：Multi-Agent / Skill / 业务落地 —— JD 的差异化要求 ============ */

NOTES.push(
  {
    id: "kb-multiagent-comm",
    cat: "Multi-Agent",
    title: "多智能体的通信与协作机制",
    core: true,
    q: "JD 要求「解决智能体间的通信、协作与竞争机制」，怎么答？",
    a: `- **通信方式**三种：
  - **共享状态**（黑板模式）：所有 Agent 读写同一个 State 对象。简单，但要防写冲突
  - **消息传递**：Agent 之间发消息（类似 actor 模型），解耦但要设计消息协议和路由
  - **中心调度**（Supervisor）：一个 Supervisor 决定下一步交给谁，子 Agent 之间不直接通信。**最可控，推荐默认选这个**
- **协作模式**：
  - **流水线**：固定顺序传递（研究员 → 写作者 → 审校）
  - **分工并行**：拆成独立子任务同时做，最后汇总
  - **辩论/投票**：多个 Agent 各出方案再评判，成本高但质量高
- **竞争/冲突**怎么解：给出**明确的仲裁规则**——Supervisor 拍板、投票多数、或按置信度排序。别让 Agent 自己「商量」，会无限循环
- **必须有的护栏**：全局最大轮数、全局 token 预算、检测「两个 Agent 互相甩锅」的循环
- 务实结论：**单 Agent + 好工具能解决的绝不上多 Agent**——成本指数增长、调试噩梦、失败归因困难
- 面试点：能主动说「我先判断能不能用单 Agent 解决」+ 能说出 Supervisor 模式最可控，比列举框架强`,
  },
  {
    id: "kb-agent-skill",
    cat: "Multi-Agent",
    title: "Skill 是什么，怎么设计",
    core: true,
    q: "JD 反复提「Skill 设计、工具封装」，Skill 和 Tool 什么区别？",
    a: `- **Tool** 是一个原子函数（查数据库、发请求）；**Skill** 是**面向一类业务任务的能力包**：包含若干 tool + 专用 prompt + 执行流程 + 结果校验
- 举例：「合同审查」是一个 Skill，内部含 解析文档 / 抽关键条款 / 比对模板库 / 生成风险清单 四个步骤和多个 tool
- Skill 设计的要点：
  - **单一职责**：一个 Skill 解决一类任务，别做成万能入口
  - **输入输出契约明确**：用 schema 定义，方便组合和测试
  - **自带异常处理**：中间步骤失败要能降级或明确报错，不能静默返回错的
  - **结果验证**：Skill 自己要能判断产出是否合理（比如抽出的金额是不是数字、日期格式对不对）
  - **可复用**：把通用部分（文档解析、格式转换）沉淀成公共 tool
- **工具描述比实现更重要**：模型是靠 description 决定调不调、怎么传参的。描述要写清「什么时候用」「参数含义」「返回什么」，而不是复制函数名
- 面试点：**「Skill = tool + prompt + 流程 + 校验」**这个定义 + **「工具描述比实现更重要」**，能直接对上 JD 里的「Skill 设计、工具封装」`,
  },
  {
    id: "kb-agent-memory-impl",
    cat: "Multi-Agent",
    title: "Memory 系统落地",
    core: true,
    q: "JD 说「记忆系统搭建」，具体要做什么？",
    a: `- 三层结构和各自的实现：
  - **短期（会话内）**：消息列表，超窗口就摘要压缩。存 Redis，按 session_id 隔离
  - **长期（跨会话）**：把关键事实抽出来存起来。**抽取要用小模型**（「从这段对话里提取用户偏好和事实」），存向量库或结构化表
  - **工作记忆（任务内）**：当前任务的中间状态、已完成步骤、待办。存在 State 里并 checkpoint
- 长期记忆的关键设计：
  - **写入时机**：会话结束时批量抽取，别每轮都写（贵且噪音多）
  - **去重与更新**：新事实和旧事实冲突时要覆盖（「用户住北京」→「用户搬到上海」），不能两条并存
  - **检索时机**：新会话开始时按用户 id 拉一批，或按当前问题语义检索相关记忆
  - **遗忘策略**：加时间衰减或重要性打分，否则记忆库无限膨胀且噪音越来越大
- 常见错误：把所有历史都当记忆塞进 prompt —— 成本随轮次平方增长且触发 Lost in the Middle
- 面试点：说出**「冲突要覆盖不能并存」**和**「要有遗忘策略」**，说明你想过记忆库长期运行会怎样`,
  },
  {
    id: "kb-biz-decompose",
    cat: "业务落地",
    title: "把业务需求拆成 AI 可执行步骤",
    core: true,
    q: "JD 说「能将复杂业务问题拆解为 AI 可执行步骤」，方法论是什么？",
    a: `- 拆解四步：
  - **① 明确产出物**：用户最终要什么（一份报告？一个数字？一个操作？）。产出物决定整条链路
  - **② 倒推所需信息**：要生成这个产出，需要哪些输入、从哪来（用户提供 / 检索 / 调接口 / 计算）
  - **③ 判断每步谁做**：**规则能做的绝不给模型**（金额计算、格式校验、权限判断），模型只做「理解语义」和「生成文本」这两件它真擅长的事
  - **④ 设计失败路径**：每一步失败怎么办——重试、降级、还是明确告诉用户做不到
- 关键判断：**「什么时候 AI 不适合回答」**（这是 JD 原话）
  - 需要精确计算和统计 → 走 SQL / 代码，不让模型算
  - 涉及合规、法律、医疗结论 → 只做信息汇总，不给结论
  - 知识库里没有依据 → 明确说不知道，别让它编
  - 需要实时数据 → 走接口，不靠模型记忆
- 反面模式：把整个业务流程丢给一个大 prompt 让模型「自己想办法」
- 面试点：**「规则能做的绝不给模型」+「明确 AI 不适合的场景」**——这两句是产品思维的证明，比技术细节更能区分候选人`,
  },
  {
    id: "kb-biz-rollout",
    cat: "业务落地",
    title: "AI 功能怎么在企业内推起来",
    core: true,
    q: "技术做完了但业务部门不用，怎么办？",
    a: `- 常见原因：**不准**（一次答错就失去信任）、**不快**（比自己查还慢）、**不会用**（不知道怎么问）、**没嵌进工作流**（要额外打开一个系统）
- 对应做法：
  - **先窄再宽**：挑一个高频、容错高、边界清楚的场景做到能用，拿到第一批正面反馈再扩
  - **嵌入现有系统**：在他们已经用的 OA / 工单 / IM 里出现，而不是让他们记住新网址
  - **给提示词模板**：内置常见问法的快捷入口，降低「不知道怎么问」的门槛
  - **明确边界**：答不了就说答不了，并给出人工入口。**可靠的「不知道」比不可靠的答案更能建立信任**
  - **展示依据**：把引用的原文和出处给出来，用户能自己核验
- 反馈闭环：**收集点踩和 bad case，回流到评测集**，让改进有依据而不是凭感觉
- 面试点：能说出「**可靠的不知道比不可靠的答案更有价值**」和「嵌入现有工作流」，说明你交付过给真实业务用的东西`,
  },
  {
    id: "kb-ai-native-dev",
    cat: "业务落地",
    title: "AI-Native 开发（用 AI 写代码）",
    core: true,
    q: "JD 要求「能对 AI 生成代码进行架构设计、审查和工程化改造」，怎么答？",
    a: `- 这条要求的实质：**你是用 AI 提效的工程师，不是把 AI 输出直接提交的搬运工**
- 我的实际做法：
  - **架构和边界我定，实现交给 AI**：先想清模块划分和接口契约，再让 AI 填实现
  - **小步验证**：每次让它改一小块并立刻跑测试，不要一次生成几百行
  - **必审的地方**：错误处理是否被简化掉、边界条件、并发安全、有没有引入不必要的依赖、有没有硬编码
  - **AI 的典型毛病**：过度封装、加不需要的抽象层、错误处理写成 ~except: pass~、编造不存在的 API
  - **测试不能让它一个人写**：AI 写的测试常常只测 happy path，且容易「按实现写测试」而不是按需求
- 可复用的沉淀：把项目约定写成规则文件（架构说明、命名规范、禁用模式），让 AI 每次都遵守，而不是每次重复交代
- 面试点：能说出**「架构我定、实现交给它」+ 具体的 AI 代码通病**，这题就答满了。说「我用 Cursor 很熟」是最差的答法`,
  },
);

QUESTIONS.push(
  {
    id: "q91",
    cat: "Agent",
    q: "多 Agent 之间怎么通信和协作？冲突怎么解决？",
    a: "通信三种：**共享状态**（黑板模式，简单但要防写冲突）、**消息传递**（解耦但要设计协议）、**中心调度 Supervisor**（子 Agent 不直接通信，最可控，我会默认选这个）。协作模式有流水线、分工并行、辩论投票。**冲突必须有明确仲裁规则**——Supervisor 拍板、投票多数、或按置信度排，别让 Agent 自己商量会无限循环。护栏是全局最大轮数、token 预算、互相甩锅的循环检测。但我的前提判断是**单 Agent + 好工具能解决的绝不上多 Agent**，成本指数增长且失败归因困难。",
  },
  {
    id: "q92",
    cat: "Agent",
    q: "Skill 和 Tool 有什么区别？Skill 怎么设计？",
    a: "**Tool 是原子函数**（查库、发请求），**Skill 是面向一类业务任务的能力包**——包含若干 tool + 专用 prompt + 执行流程 + 结果校验。比如「合同审查」这个 Skill 内含解析文档、抽条款、比对模板、生成风险清单四步。设计要点：单一职责、输入输出用 schema 定契约、自带异常处理、**Skill 自己要能验证产出合理性**（金额是不是数字、日期格式对不对）、通用部分沉淀成公共 tool。还有一条常被忽略：**工具描述比实现更重要**，模型是靠 description 决定调不调和怎么传参的。",
  },
  {
    id: "q93",
    cat: "Agent",
    q: "Agent 的记忆系统具体怎么落地？",
    a: "三层：**短期**是会话内消息列表，超窗口就摘要压缩，存 Redis 按 session 隔离；**长期**跨会话，用小模型从对话里抽关键事实存向量库或结构表；**工作记忆**是任务内的中间状态，存 State 并 checkpoint。长期记忆的四个关键设计：**写入时机**（会话结束批量抽，别每轮都写）、**冲突要覆盖不能并存**（「住北京」→「搬到上海」）、**检索时机**（新会话按用户拉一批或按语义检索）、**遗忘策略**（时间衰减或重要性打分，否则记忆库无限膨胀噪音越来越大）。",
  },
  {
    id: "q94",
    cat: "工程",
    q: "怎么把一个复杂业务需求拆成 AI 可执行的步骤？",
    a: "四步：**① 明确产出物**（用户最终要什么，它决定整条链路）；**② 倒推所需信息**（哪些输入、从哪来）；**③ 判断每步谁做**——**规则能做的绝不给模型**，金额计算、格式校验、权限判断都用代码，模型只做「理解语义」和「生成文本」；**④ 设计失败路径**（重试、降级、还是明确告知做不到）。反面模式是把整个业务流程丢给一个大 prompt 让模型自己想办法。",
  },
  {
    id: "q95",
    cat: "工程",
    q: "什么时候 AI 不适合回答？",
    a: "四类必须绕开：**需要精确计算统计**（走 SQL 或代码，别让模型算）、**涉及合规法律医疗结论**（只做信息汇总不给结论）、**知识库里没依据**（明确说不知道，别让它编）、**需要实时数据**（走接口，不靠模型记忆）。工程上的体现是路由判断和兜底设计。这题背后是产品思维——**能主动划出 AI 的能力边界，比声称什么都能做更可信**。",
  },
  {
    id: "q96",
    cat: "工程",
    q: "AI 功能上线了但业务部门不用，怎么办？",
    a: "先归因：**不准**（一次答错就失去信任）、**不快**（比自己查还慢）、**不会用**（不知道怎么问）、**没嵌进工作流**（要额外开一个系统）。对应做法：**先窄再宽**（挑高频、容错高、边界清楚的场景做到能用再扩）、**嵌入现有系统**（在 OA/工单/IM 里出现）、**给提示词模板**降低提问门槛、**明确边界**答不了就给人工入口、**展示引用依据**让用户能自己核验。核心认知是**可靠的「不知道」比不可靠的答案更能建立信任**。",
  },
  {
    id: "q97",
    cat: "工程",
    q: "你怎么用 AI 写代码？生成的代码怎么审？",
    a: "**架构和边界我定，实现交给 AI**——先想清模块划分和接口契约再让它填实现，小步验证每次改一小块立刻跑测试。必审的点：错误处理有没有被简化掉、边界条件、并发安全、有没有引入不必要依赖、有没有硬编码。**AI 的典型毛病**：过度封装、加不需要的抽象层、错误处理写成 except: pass、编造不存在的 API。测试不能让它一个人写——AI 写的测试常常只测 happy path，而且容易「按实现写测试」而不是按需求。可复用的沉淀是把项目约定写成规则文件让它每次遵守。",
  },
);
