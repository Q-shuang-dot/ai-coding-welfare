/* ============ 追加批次 4：本次实测沉淀的工程/安全/供应商治理知识点 ============ */

// ---- 工程与推理 ----
NOTES.push(
  {
    id: "kb-tools-passthrough",
    cat: "工程与推理",
    title: "怎么验证中转站真的转发了 tools",
    core: true,
    q: "上游声称支持 Function Calling，怎么在不猜的前提下证伪？",
    a: `- 症状：模型回「我没有 XX 这个工具」，~tool_calls~ 是 null，但 HTTP 200，**故障转移救不了**（不是错误码）
- **硬判据：同一请求发两次，一次带 tools 一次不带，比 ~prompt_tokens~ 差值**
- 三档结论（实测数据）：
- 差值 **+7400** → 真转发（单函数定义在 claude 系约占 7400 tok）
- 差值 **+40** → 转发了，但模型本身没有工具调用能力
- 差值 **0** → 中转层就把 tools 剥掉了，模型压根没看见
- 面试点：这比「感觉它不听话」可靠得多，和「核对响应体 model 字段查换模型」是同一类手段——**用可观测的数字证伪供应商的声明**`,
  },
  {
    id: "kb-webui-backend",
    cat: "工程与推理",
    title: "中转站接的是 API 还是网页版后端",
    core: true,
    q: "为什么有的中转站便宜但「干不了活」？",
    a: `- 有的中转站转的不是官方裸 API，而是**消费者网页版后端**（如 Claude.ai），成本低但能力被阉割
- 识别信号（实测）：
- 响应体 ~usage.usage_source~ = ~anthropic~、~billing_usage.source~ = ~claude_messages~
- 问它 system 指令，自述「我可以搜索网页、生成图片」= 消费者产品提示词
- 极短问题也报 **780 prompt_tokens**（隐藏 system prompt 的体积）
- 后果：那段隐藏 prompt 自带 web_search / image_gen 工具，**占了工具位把你的 tools 挤掉**
- 关键判断：**纯对话能力完全正常**（system prompt 生效、能找 bug、能写代码）→ 不是降智，是通道不对
- 面试点：能区分「模型弱」和「通道阉割」，说明你会看协议层证据而不是凭感觉`,
  },
  {
    id: "kb-capability-routing",
    cat: "工程与推理",
    title: "按能力分流（vision / noTools）",
    core: true,
    q: "上游便宜但缺某个能力，是不是只能整条删掉？",
    a: `- 不是。给 route 打**能力标记**，按请求实际需要什么来分流同一个上游：
- ~vision: true~ = 图片专用（贵，仅带图请求走）；~false~ = 不接图（带图时先剥图再转发）
- ~noTools: true~ = 会丢 tools（带 tools 的请求绕开它，纯对话请求照常兜底）
- 请求侧判定：~has_image()~ 只看**最后一条 user 消息**（否则历史贴过图后每轮纯文字都被误判成有图）；~has_tools()~ 看 tools 数组非空
- 兜底原则：若某能力把所有路由都过滤光了，**忽略该限制并打日志**，好过直接报 502
- 收益量化：实测 VS Code agent 90% 请求带 tools，只有 10% 是纯对话——所以 noTools 上游只能吃到那 10%，但**那 10% 本来是白花钱的**
- 面试点：这是「能力协商」的朴素实现，比「一个上游只能整体启用/禁用」精细一档`,
  },
  {
    id: "kb-agent-tool-cost",
    cat: "工程与推理",
    title: "Agent 模式的隐形成本：工具定义",
    core: true,
    q: "为什么 agent 比普通对话贵那么多？",
    a: `- 抓包实测：VS Code agent 每轮请求携带 **80 个工具定义 ≈ 25000 tokens**，且**每轮都重发**，一次不落
- 这意味着：一次 20 轮的 agent 任务，光工具定义就是 50 万 input token
- 优化方向：按任务裁剪工具集、利用 prompt caching（工具定义在最前面且不变，缓存命中率高）
- 面试点：能说出「agent 的成本大头常常不是对话历史而是工具定义 + 多轮累积」，说明你真看过线上账单
- 顺带：这也是为什么 ~noTools~ 上游对 agent 场景没用——**它连那 25000 tok 都收不到**`,
  },
  {
    id: "kb-dead-route",
    cat: "工程与推理",
    title: "死路由与定期审计",
    core: true,
    q: "配置里的路由会自己坏掉，怎么发现？",
    a: `- 上游会**动态下架模型 / 欠费 / 换 key**，死路由让每次请求先白试 N 家（各等一次超时或错误码再跳）
- ~GET /models~ 不占 chat 配额，可随时探测；但**列出来 ≠ 真能调**（实测有模型在列表里却回 ~model_not_found~）
- 所以要两层审计：~/models~ 看声明 + 发一次最小真实请求看实际
- 一次审计能查出的典型问题（实测）：余额为 0（402）、key 失效（401）、整批前缀 503、上下文上限被砍到 256 tok
- 面试点：把「配置腐化」当成必然会发生的事去设计，而不是出故障了才查`,
  },
  {
    id: "kb-state-merge",
    cat: "工程与推理",
    title: "双端进度同步的覆盖陷阱",
    core: true,
    q: "本地 + 服务端两份状态，为什么「谁非空用谁」是错的？",
    a: `- 错误写法：拉到服务端数据只要非空就**整体替换**本地，然后立刻回写 → 旧快照把新进度冲掉且**无法回滚**
- 正确做法：状态带 ~updatedAt~ 时间戳，做**字段级合并**，同一 key 冲突时取时间戳较新的一方
- 没时间戳的历史数据视为「更旧」，避免它赢过有时间戳的新数据
- 合并后与原值相同就不触发写入，防止无意义的同步风暴
- 面试点：这就是最朴素的 last-write-wins；能主动说出「整体替换会丢数据」说明你想过并发`,
  },
);

// ---- 安全 ----
NOTES.push(
  {
    id: "kb-sec-render",
    cat: "安全",
    title: "把模型输出渲染成 HTML 的风险",
    core: true,
    q: "自己写的 markdown 渲染器为什么也算漏洞？",
    a: `- 迷你渲染器常见写法：正则替换后直接塞 ~innerHTML~ → **没转义就是 XSS**
- 「内容是我自己写的」不是理由：一旦加了笔记输入框、或内容来自模型输出/检索结果，立刻变成真漏洞
- 修法：**先 escape 再做 markdown 替换**（注意 escape 后 ~>~ 变成 ~&gt;~，引用语法的正则要跟着改）
- 更广的原则：**模型输出属于不可信输入**，和用户输入同级对待
- 面试点：LLM 应用的 XSS 面比传统 Web 更大，因为「内容由模型生成」容易让人误以为可信`,
  },
);

// ---- 微调与评测 ----
NOTES.push(
  {
    id: "kb-vendor-audit",
    cat: "微调与评测",
    title: "供应商能力评估清单",
    core: true,
    q: "接一个新中转站，要按什么顺序验什么？",
    a: `- 逐项过（实测总结的顺序，从便宜到贵）：
- ~GET /models~ 看声明（免费）
- 发一次最小 chat，**核对响应体 ~model~ 字段**是否被静默替换
- 带 tools 发一次，比 ~prompt_tokens~ 差值判断是否真转发
- 带图发一次，确认是真看图还是静默忽略回「无图片」
- 查余额端点（new-api 系：~/dashboard/billing/usage~ 与 ~/subscription~）
- 同一句话不同措辞各发一次，看隐藏 system prompt 的体积是否稳定
- 常见坑：多数站要带 ~User-Agent~，否则 Cloudflare 直接 403
- **同名模型不同上游能力不一致**是常态（同一个 ~glm-5.3~ 一家能看图一家 400 拒图）
- 面试点：这份清单本身就是「供应商治理」的产出物，比说「我会评估供应商」具体得多`,
  },
  {
    id: "kb-eval-real-task",
    cat: "微调与评测",
    title: "用真实任务而不是玩具题评测",
    core: true,
    q: "为什么合成测试题会把你带偏？",
    a: `- 实测反例：数字点阵图测视觉，某模型 3 轮只中 1 轮；但换成**真实业务截图**（大字号中文 + 表格）反而全对
- 反过来也有：纯色图能过、精细数字读错 → **合成题的难度分布和真实场景不重合**
- 找 bug 题的设计要点：埋一个**只在特定输入下触发**的隐蔽逻辑错误（如 ~if k~ 把 ~k=0~ 判成假），比「这段代码有什么问题」有区分度
- 一道好题能同时暴露：能力差异、延迟差异、是否被换后端
- 面试点：把「评测集要覆盖真实分布」从口号变成具体教训——你有反例`,
  },
  {
    id: "kb-thinking-model",
    cat: "微调与评测",
    title: "思考模型的 max_tokens 陷阱",
    core: true,
    q: "为什么有的模型返回空内容但不报错？",
    a: `- 思考模型（返回 ~reasoning_content~）会**先烧 token 想，再输出正文**
- ~max_tokens~ 给太小 → 全烧在 reasoning 上，~content~ 空、~finish_reason~ 是 length，**不报错**
- 实测：某 flash 模型 400 tok 不够、64 tok 必空；给 512+，复杂任务 1024+
- 识别方法：响应里有 ~reasoning_content~ 或 ~completion_tokens_details.reasoning_tokens~ 就是思考模型
- 面试点：这类「成功返回但结果为空」的静默失败，和 deepseek 静默丢图是同一类问题——**HTTP 200 不代表业务成功**`,
  },
);

// ============================== 面试 Q&A 补充 ==============================

QUESTIONS.push(
  {
    id: "q25",
    cat: "工程",
    q: "接了一个新的模型中转站，你怎么判断它有没有虚标能力？",
    a: "按成本从低到高验：先 **GET /models** 看声明（免费）；再发一次最小 chat **核对响应体 model 字段**是否被静默替换；带 tools 发一次，**比对带/不带 tools 的 prompt_tokens 差值**（差值 0 = 中转层剥掉了，差值几千 = 真转发）；带图发一次确认是真看图还是静默回「无图片」；最后查余额端点。核心思路是**用可观测的数字证伪，不靠感觉**。",
  },
  {
    id: "q26",
    cat: "工程",
    q: "上游返回 HTTP 200，但结果不对，你的网关怎么处理？",
    a: "故障转移只在 HTTP 错误时触发，所以这类**静默失败必须在请求前分流，而不是靠事后转移**。我的做法是给 route 打能力标记：不接图的标 vision:false（转发前先剥图）、会丢 tools 的标 noTools（带 tools 的请求绕开）。三类典型静默失败：模型收到图不报错只回「没有图片」、中转层剥掉 tools 参数、思考模型 max_tokens 不足返回空 content。",
  },
  {
    id: "q27",
    cat: "工程",
    q: "有个上游很便宜但缺一项能力，你会怎么用它？",
    a: "不整条删掉，而是**按请求实际需要什么来分流**。比如某站不支持 tools，就标 noTools 放队尾——带 tools 的 agent 请求自动绕开，纯对话请求照常兜底。这样便宜的容量不浪费，能力缺口也不影响主流程。前提是要量化收益：我抓包发现 VS Code agent 90% 请求带 tools，所以这个上游只能吃到 10%——但那 10% 本来是花贵价钱的。",
  },
  {
    id: "q28",
    cat: "工程",
    q: "Agent 模式的成本大头在哪？",
    a: "很多人以为是对话历史，其实**工具定义常常更贵**。我抓包实测 VS Code agent 每轮携带 80 个工具定义约 25000 tokens，且每轮都重发。一次 20 轮的任务光工具定义就是 50 万 input token。优化方向：按任务裁剪工具集、利用 prompt caching（工具定义在最前面且不变，缓存命中率高）。",
  },
  {
    id: "q29",
    cat: "工程",
    q: "配置里的路由会自己失效，你怎么发现？",
    a: "把配置腐化当成必然发生的事。两层审计：GET /models 看声明（不占 chat 配额，可随时跑）+ 发一次最小真实请求看实际能不能调——因为**列在 /models 里不等于真能用**，我实测过模型在列表里却回 model_not_found。一次审计查出的典型问题：余额为 0（402）、key 失效（401）、整批模型前缀 503、上下文上限被砍到 256 tok。",
  },
  {
    id: "q30",
    cat: "评测",
    q: "你的评测题是怎么设计的？",
    a: "两个原则。一是**用真实任务而不是合成题**：我用数字点阵图测视觉，某模型 3 轮只中 1 轮，但换成真实业务截图（大字号中文+表格）反而全对——合成题的难度分布和真实场景不重合。二是**埋只在特定输入下触发的隐蔽 bug**，比如 ~if k~ 把 k=0 判成假，比问「这段代码有什么问题」有区分度。一道好题能同时暴露能力差异、延迟差异和是否被换后端。",
  },
  {
    id: "q31",
    cat: "安全",
    q: "你自己写的 markdown 渲染器有什么安全隐患？",
    a: "正则替换后直接塞 innerHTML 就是 XSS。「内容是我自己写的」不是理由——一旦加了输入框、或内容来自模型输出和检索结果，立刻变成真漏洞。修法是**先 escape 再做 markdown 替换**（注意 escape 后 > 变成 &gt;，引用语法的正则要跟着改）。更广的原则：**模型输出属于不可信输入**，和用户输入同级对待。",
  },
  {
    id: "q32",
    cat: "工程",
    q: "本地和服务端两份进度数据，怎么同步才不丢？",
    a: "不能「谁非空用谁」——那样旧快照会整体覆盖新进度且无法回滚。正确做法是状态带 updatedAt 时间戳，做**字段级合并**，同一 key 冲突时取时间戳较新的一方；没时间戳的历史数据视为更旧。另外合并结果与原值相同就不触发写入，防同步风暴。这就是最朴素的 last-write-wins。",
  },
);

// ============================== JD 翻译对照表补充 ==============================

JDMAP.push(
  { jd: "供应商能力验证", your: "带/不带 tools 比对 prompt_tokens 差值证伪「支持 Function Calling」的声明；核对响应体 model 字段查静默换后端。", level: "核心" },
  { jd: "能力协商 / 分级路由", your: "route 打 vision 与 noTools 标记，按请求是否带图/带工具自动分流同一上游，缺能力的便宜上游只承接它能干的活。", level: "核心" },
  { jd: "成本分析", your: "抓包量化 agent 每轮 80 个工具定义约 25000 tok；统计带 tools 请求占比 90%，据此决定各上游能承接的流量。", level: "核心" },
  { jd: "配置治理 / 巡检", your: "两层审计（/models 看声明 + 最小真实请求看实际）发现死路由、欠费、key 失效、上下文被砍。", level: "加分" },
  { jd: "前端安全", your: "迷你 markdown 渲染器先 escape 再替换，把模型输出当不可信输入处理。", level: "加分" },
  { jd: "数据一致性", your: "双端进度同步用 updatedAt 时间戳做字段级合并，避免旧快照整体覆盖。", level: "加分" },
);

