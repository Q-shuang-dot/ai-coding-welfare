/* ============ 追加批次 2：Agent/工具 与 工程/推理（知识点卡片） ============ */

// ---- Agent 与工具 ----
NOTES.push(
  {
    id: "kb-funccall",
    cat: "Agent 与工具",
    title: "Function Calling 是什么",
    core: true,
    q: "Function Calling 和「让模型输出 JSON」有什么区别？",
    a: `- 本质：在 API 层用 ~tools~ 参数声明函数，模型在生成时被约束为**输出「调哪个函数 + 什么参数」的结构化结果**，而不是自由文本
- 调用链：客户端声明 tools → 模型返回 ~tool_calls~（含 id、函数名、参数）→ 你执行真实函数 → 把结果以 ~role: tool~ 且 ~tool_call_id~ 对应的消息回传 → 模型继续
- 面试点：Function Calling 是协议不是魔法——**执行权永远在你手里**，模型只负责决策`,
  },
  {
    id: "kb-tool-fallback",
    cat: "Agent 与工具",
    title: "tools 降级模拟",
    core: true,
    q: "上游不支持 tools 参数怎么办？",
    a: `- 降级路径：把 tools 定义序列化进 system prompt（函数名、参数、说明），要求模型只输出 JSON
- 再把模型的 JSON 解析回标准 ~tool_calls~ 结构，客户端无感
- 坑：不支持的上游可能**静默忽略** tools 参数当普通文本生成 → 结果无法解析，必须兜底
- 面试点：你的网关会按 ~supportsTools~ 优先选原生上游，模拟路径只兜底——这是「能力协商」设计`,
  },
  {
    id: "kb-tool-parallel",
    cat: "Agent 与工具",
    title: "并行工具调用",
    core: true,
    q: "模型一次返回多个 tool_calls 怎么处理？",
    a: `- 模型可以一次返回多个 ~tool_calls~（数组），并行执行真实函数
- 回传时必须**每个结果都用对应的 ~tool_call_id~ 回**，错位会导致模型混乱或报错
- 顺序依赖的任务别并行：B 依赖 A 的结果时，只能等 A 完成再下一轮`,
  },
  {
    id: "kb-structured-output",
    cat: "Agent 与工具",
    title: "结构化输出与 JSON 解析",
    core: true,
    q: "让模型输出 JSON 老是带废话怎么办？",
    a: `- 别指望「请只输出 JSON」就够：模型会加 markdown 代码块、前言、多打一个逗号
- 容错解析链：剥代码块围栏 → 定位第一个 { 到最后一个 } → json.loads → 失败重试一次 → 再失败按错误提示模型重生成
- 更强方案：JSON mode / 约束解码（在采样层强制合法 JSON，如 vLLM 的 guided decoding）
- 面试点：能说出「我在采样层约束 vs 在解析层容错」的区别，就是内行`,
  },
  {
    id: "kb-react",
    cat: "Agent 与工具",
    title: "ReAct 循环",
    core: true,
    q: "Agent 是怎么自己干活的？",
    a: `- ReAct = Reason + Act：模型每轮输出「思考 → 动作 → 观察结果」再思考……
- 伪代码：while 未完成 and 步数未超限: 调模型 → 解析动作 → 执行工具 → 观察结果回填消息历史
- 关键：**每一步的观察结果都要进消息历史**，模型才能基于事实继续
- 面试点：先手写 50 行 ReAct，再上 LangGraph，你就永远不会被框架绑架`,
  },
  {
    id: "kb-langgraph",
    cat: "Agent 与工具",
    title: "LangGraph 的核心概念",
    core: true,
    q: "LangGraph 比 LangChain 的 Chain 强在哪？",
    a: `- 三个概念：**State**（各节点共享的状态对象）、**Node**（一个处理步骤）、**Edge**（节点间的流转，条件边决定下一步）
- 循环是显式的：Agent 的「多轮工具调用」在 LangGraph 里就是一条回到自己的边
- Checkpointer：保存每一步的状态快照 → 支持断点续跑、人工介入、可回放
- 面试点：说出「Chain 是线性、Graph 是环」+ Checkpointer 的用途，就证明你真用过`,
  },
  {
    id: "kb-agent-guard",
    cat: "Agent 与工具",
    title: "Agent 防护栏（防死循环）",
    core: true,
    q: "Agent 卡在循环里疯狂调工具怎么办？",
    a: `- 必备护栏：最大步数、总 token 上限、单工具超时、最大成本上限
- 检测重复：同样的「工具+参数」出现 N 次就中断，提示模型换策略
- 人工确认：高危动作（删数据、发消息、付款）永远要 human-in-the-loop
- 面试点：Agent 的坑是**必然会发生**的，面试官要听的是你加了几层护栏，而不是「我的 Agent 很稳」`,
  },
  {
    id: "kb-mcp",
    cat: "Agent 与工具",
    title: "MCP（模型上下文协议）",
    core: true,
    q: "MCP 解决什么问题？",
    a: `- 标准化「工具/资源/提示词」的接入方式：一个工具写一次，所有支持 MCP 的客户端都能用
- 类比：MCP 之于工具，就像 USB-C 之于接口——统一插口
- 面试点：知道 MCP 是协议层、与「你的网关的 /v1 兼容层」是不同层级的东西，别混`,
  },
  {
    id: "kb-multiagent",
    cat: "Agent 与工具",
    title: "多 Agent 协作的坑",
    core: true,
    q: "多个 Agent 分工是不是更厉害？",
    a: `- 经典结构：Supervisor 分派任务给子 Agent，子 Agent 汇报结果
- 坑：Agent 越多越难控制、成本指数增长、调试变成噩梦、互相甩锅
- 工程结论：**能用单 Agent + 好工具解决的，绝不上多 Agent**；面试里主动说这句反而加分`,
  },
);

// ---- 工程与推理 ----
NOTES.push(
  {
    id: "kb-gateway",
    cat: "工程与推理",
    title: "LLM 网关的价值",
    core: true,
    q: "为什么需要网关这一层？",
    a: `- 统一 OpenAI 协议：客户端只认一个地址，底层换上游无感
- 集中管理：密钥不散落各端、一个地方做限流/熔断/审计
- 路由自由：按模型、成本、可用性、图片需求分流；A/B 灰度换模型
- 可观测：一次调用打哪个上游、真实模型是什么、花了多少钱
- 面试点：你的 gcmp-gateway 就是这个，讲价值时用「成本 + 可用性 + 可观测」三件套`,
  },
  {
    id: "kb-failover",
    cat: "工程与推理",
    title: "故障转移设计",
    core: true,
    q: "上游挂了怎么做到业务无感？",
    a: `- 每个逻辑模型配多个上游候选，失败按序退避，最后兜底
- 关键坑：**响应头一旦发给客户端就不能再转移**——所以流式必须读到第一个有内容的块才提交响应头，否则客户端已经看到 200 了
- 失败要先判断类型：429 等限流可快速跳过、超时/断连可重试、4xx 业务错直接放弃
- 面试点：这个「流式响应提交时机」的细节，是最能证明你真做过的地方`,
  },
  {
    id: "kb-circuit",
    cat: "工程与推理",
    title: "熔断器",
    core: true,
    q: "熔断和重试的区别？",
    a: `- 重试是对单次请求；熔断是**对整条路由的节流**：连续失败 N 次 → 打开，冷却期内直接跳过，不再拿真实请求去试错
- 半开状态：冷却结束后放少量请求试探，成功才恢复，失败则重新熔断
- 关键坑：**半开失败不能归零失败计数**，否则死路由每轮都要白试满 N 次
- 面试点：三态（关闭/打开/半开）+ 半开计数处理，是熔断器面试的标准深度`,
  },
  {
    id: "kb-ratelimit",
    cat: "工程与推理",
    title: "限流（滑动窗口）",
    core: true,
    q: "上游有配额限制，本地怎么管理？",
    a: `- 滑动窗口：记录窗口内每个请求时间戳，新请求来了清掉窗口外的旧记录再判断
- 本地限流目的：**把失败拦截在本地，不浪费真实配额**；上游真 429 时把本地窗口填满，本窗口内不再试探
- 设计取舍：还有其他上游可试 → 直接跳过；只剩最后一家 → 才排队等窗口滑出
- 面试点：说「本地限流是为了不消耗真实配额」比说「我做了限流」高一个层次`,
  },
  {
    id: "kb-sticky",
    cat: "工程与推理",
    title: "粘性路由",
    core: true,
    q: "为什么要有粘性上游？",
    a: `- 记录上次成功的上游，下次优先尝试 → 减少无效试探、延迟更稳定
- 坑：粘性会**覆盖配置顺序**——你按成本排了序，粘性还抓着上次那家不放 → 所以要有开关
- 面试点：能说出「粘性与配置顺序冲突 + 我给模型加了 sticky:false 开关」，说明你真在调生产路由`,
  },
  {
    id: "kb-sse",
    cat: "工程与推理",
    title: "SSE 流式输出",
    core: true,
    q: "SSE 和普通 HTTP 返回有什么区别？",
    a: `- SSE = Server-Sent Events：~text/event-stream~，每块是 ~data: {json}\\n\\n~，结束发 ~data: [DONE]~
- 上游不支持流式时，网关可以**合成 SSE**：拿到完整响应后按句子切块逐块吐出（客户端无感）
- 坑：Content-Length 与 chunked 的关系；长时间无数据时中间代理可能超时断连 → 需要心跳注释行
- 面试点：你能讲「合成 SSE」+「心跳」，就是真写过流式的人`,
  },
  {
    id: "kb-cost",
    cat: "工程与推理",
    title: "Token 计费与成本面板",
    core: true,
    q: "怎么知道每个月花在哪了？",
    a: `- 计费模型：输入 + 输出分开计价，缓存命中的输入有折扣
- 工程做法：每次调用记录 usage（prompt/completion token）+ 实际服务的模型 → 按单价折算 → 按天/模型/上游聚合
- 面试点：讲得出「按实际服务的模型计价」——因为你发现过上游偷偷换模型，账必须对着真实模型算`,
  },
  {
    id: "kb-semcache",
    cat: "工程与推理",
    title: "语义缓存",
    core: true,
    q: "语义缓存和普通缓存有什么区别？",
    a: `- 普通缓存要求 key 完全相等；语义缓存用 embedding 相似度判断「问题意思一样」
- 命中就返回缓存回答 → 不发上游 → 省 token、降延迟
- 适用：低 temperature、无副作用、问题可复用（如配置查询）
- 面试点：相似度阈值怎么定（见阈值卡）是你亲自调过的经验`,
  },
  {
    id: "kb-threshold",
    cat: "工程与推理",
    title: "相似度阈值怎么定",
    core: true,
    q: "阈值定 0.9 还是 0.95？",
    a: `- 没有标准答案，必须拿数据定：准备该命中的正例和不该命中的反例各一批，扫阈值画「准确率-阈值」曲线，取最优
- 阈值高 → 漏命中（缓存白做）；阈值低 → 错命中（答非所问，比没缓存更糟）
- 实测经验：text-embedding 系模型，中文相似问题通常 0.90~0.95 区间
- 面试点：**画过曲线的阈值**，和「我拍了个 0.9」完全不同`,
  },
  {
    id: "kb-cache-invalid",
    cat: "工程与推理",
    title: "语义缓存的失效与边界",
    core: true,
    q: "什么请求绝对不能进缓存？",
    a: `- 带图片的（看图结果与文本无关）、带 tools 的（结果有副作用）、高 temperature 的（不可复用）
- 缓存必须 TTL；涉及权限隔离（不同用户同一问题答案不同）时 key 要带用户维度
- 面试点：能主动列出「不该缓存」的边界条件 = 你真的想过缓存语义`,
  },
  {
    id: "kb-observability",
    cat: "工程与推理",
    title: "LLM 可观测性",
    core: true,
    q: "线上模型服务要盯哪些指标？",
    a: `- 请求维度：成功率、首 token 延迟、总时长、失败原因分类（429/超时/断连/4xx）
- 模型维度：实际服务的模型（防静默换模型）、输出 token 数、每次调用的钱
- 贯穿请求 id：一次用户请求 → 网关 → 上游的完整链路可追溯
- 面试点：能列出「请求 id + 实际模型 + 成本」三件套，就是有生产经验的样子`,
  },
  {
    id: "kb-timeout",
    cat: "工程与推理",
    title: "超时要分层",
    core: true,
    q: "只设一个总超时不行吗？",
    a: `- 不行：只设总超时会拖死快速失败的上游——连接都连不上还等满 30 秒
- 分层：connect（建连）< read（读响应）< 总时长（含重试），上游 429 时短重试、快失败
- 面试点：说出「connect/read/总时长分层 + 快失败」这个组合，面试官就知道你被超时坑过`,
  },
  {
    id: "kb-prompts-as-code",
    cat: "工程与推理",
    title: "提示词当代码管理",
    core: true,
    q: "线上提示词改了怎么保证不炸？",
    a: `- 提示词进 git、有版本号、可回滚；线上改提示词 = 发版，必须走评审
- A/B 测试：同一个请求不同提示词版本对照，用评测分决定去留
- 面试点：说「线上 prompt 改动 = 发版流程」比说「我调过 prompt」专业得多`,
  },
  {
    id: "kb-quantization",
    cat: "工程与推理",
    title: "量化（INT8/INT4）",
    core: true,
    q: "量化省了什么、损失了什么？",
    a: `- 把权重从 FP16 压到 INT8/INT4：显存降约一半/四分之三，推理变快
- 损失：精度微降；敏感任务（数学、代码）可能明显劣化
- 面试点：接上游时核对「是不是量化版」（你实测过 qwen3.8-27b-awq），这是供应商评估的一部分`,
  },
  {
    id: "kb-vllm",
    cat: "工程与推理",
    title: "vLLM 的关键优化",
    core: true,
    q: "本地部署推理为什么首选 vLLM？",
    a: `- PagedAttention：像操作系统分页一样管理 KV Cache，显存利用率大幅提升
- Continuous batching：新请求到了不用等当前批次完，动态插入 → 吞吐成倍
- 面试点：JD 里「熟悉 vLLM」只需说清这两点 + 一句话「prefill/decode 分离、chunked prefill 进一步降低首 token 延迟」，就够聊了`,
  },
);
