/* ============ 追加批次 9：面试题扩容（补齐类别失衡 + 高频真题） ============ */

QUESTIONS.push(
  // ---- 理论（原本只有 5 道，八股是筛人门槛） ----
  {
    id: "q46",
    cat: "理论",
    q: "Transformer 里为什么要有 LayerNorm 和残差连接？",
    a: "残差连接（~x + f(x)~）让梯度能直接回传到浅层，否则几十层堆下去梯度会消失，深网络根本训不动。LayerNorm 把每一层的激活值归一化，稳住训练不发散。补一个细节能加分：现在主流是 **Pre-LN**（归一化放在子层之前）而不是原论文的 Post-LN，因为 Pre-LN 训练更稳、不需要精细的 warmup；代价是最终效果略差一点，所以有些实现会用 Sandwich-LN 折中。",
  },
  {
    id: "q47",
    cat: "理论",
    q: "为什么 Attention 公式里要除以 √d？",
    a: "Q 和 K 做点积，维度 d 越大内积的方差越大（方差正比于 d），数值一大 softmax 就会被推向 one-hot——梯度几乎为 0，训不动。除以 ~√d~ 把方差拉回 1 附近，让 softmax 保持在有梯度的区间。这题是「你是背过公式还是理解公式」的分水岭，能说出「防止 softmax 饱和导致梯度消失」就够了。",
  },
  {
    id: "q48",
    cat: "理论",
    q: "Encoder-only、Decoder-only、Encoder-Decoder 分别适合什么？",
    a: "Encoder-only（BERT 系）双向看全文，适合分类、抽取、做 embedding；Decoder-only（GPT 系）单向因果注意力，适合生成，现在的大模型几乎都是这个；Encoder-Decoder（T5 系）适合翻译、摘要这类明确的序列到序列任务。**为什么 Decoder-only 赢了**：训练目标简单统一（预测下一个 token）、能无监督吃海量数据、涌现能力更强，而且 in-context learning 让它不微调也能干各种活。",
  },
  {
    id: "q49",
    cat: "理论",
    q: "什么是涌现能力，它可靠吗？",
    a: "涌现指某些能力（多步推理、指令遵循）在模型规模跨过某个门槛后突然出现，小模型完全不会。但要给出批判视角才显水平：**有研究认为「涌现」部分是评价指标造成的假象**——用「完全正确才得分」这种非连续指标会显得突然跳变，换成连续指标（如 token 级概率）曲线其实是平滑的。工程上的含义：**不要指望换更大模型就自动解决问题，先做评测确认能力真的存在**。",
  },
  {
    id: "q50",
    cat: "理论",
    q: "CoT（思维链）为什么有效？现在的推理模型和它什么关系？",
    a: "CoT 让模型把中间步骤写出来，本质是**用输出 token 换计算深度**——每个 token 都是一次前向，写得越多相当于「想」得越久。所以它对多步算术、逻辑推理提升明显，对简单事实召回没用。现在的推理模型（o 系、DeepSeek-R1 类）是把 CoT 内化进训练：用强化学习让模型自己学会长链思考，输出 ~reasoning_content~ 和正文分离。**工程坑：这类模型 max_tokens 给小了会全烧在 reasoning 上返回空正文**。",
  },

  // ---- RAG ----
  {
    id: "q51",
    cat: "RAG",
    q: "文档怎么切块？有标准答案吗？",
    a: "没有标准答案，但有判断顺序。**先按结构切**（markdown 标题、代码块、表格边界），结构切不动再按语义（段落、句子），最后才是固定长度兜底。块间留 10~15% 重叠保住边界语义。进阶做法是**父子分块**：用小块做检索（精度高），命中后把它所属的大块喂给模型（上下文完整）。表格和代码**绝对不能中间切断**，否则检索到一半的表格模型没法用。",
  },
  {
    id: "q52",
    cat: "RAG",
    q: "多轮对话下 RAG 怎么做检索？",
    a: "直接拿最后一句去检索会挂——用户说「那它的价格呢」，这句话单独拿出来毫无信息量。必须先做**查询改写**：把最近几轮历史给模型，让它重写成一个自包含的独立问题（「产品 X 的价格是多少」）。改写用小模型就够，成本低。进阶可以生成多个查询变体并行检索再合并（RAG-Fusion）。这是多轮 RAG 和单轮 RAG 最大的工程差别。",
  },
  {
    id: "q53",
    cat: "RAG",
    q: "RAG 的权限控制怎么做？",
    a: "**必须在检索层做，不能靠 prompt**。做法是把权限标签写进向量库的元数据，检索时带 filter 条件，用户无权看的文档根本不进候选集。绝对不能「全量检索完再让模型别说不该说的」——模型不可靠，而且这本质是把权限判断交给了一个概率模型。补一句：删除文档也要同步删索引，否则「已删除但仍能被检索到」是常见的合规事故。",
  },
  {
    id: "q54",
    cat: "RAG",
    q: "检索到的内容互相矛盾，模型该怎么办？",
    a: "先分清矛盾来源：**同一文档的不同版本**（要在元数据加时间戳，检索时优先新版或直接过滤旧版）、**不同来源本身就冲突**（这是数据治理问题，不是技术问题）。工程上的处理是：让模型**显式指出矛盾并给出两种说法及各自出处**，而不是自己挑一个说得斩钉截铁。这比强行让它「给个答案」安全得多，也是引用可溯源的价值所在。",
  },
  {
    id: "q55",
    cat: "RAG",
    q: "Rerank 为什么有用？不用行不行？",
    a: "向量检索是**双塔结构**，query 和 doc 分别编码再算相似度，快但精度有限；rerank 用**交叉编码器**把 query 和 doc 拼在一起过一遍模型，能捕捉细粒度匹配，精度高但慢。所以标准做法是「向量粗召回 top-50 → rerank 精排取 top-5」，兼顾速度和精度。不用也行——如果你的 Recall@5 已经够高就不必加，**先测再决定**，别默认加一层。",
  },

  // ---- Agent ----
  {
    id: "q56",
    cat: "Agent",
    q: "Agent 的记忆怎么设计？",
    a: "分三层：**短期**就是当前对话历史（受上下文窗口限制，超了要裁剪或摘要）；**长期**把关键事实抽出来存向量库或结构化存储，需要时检索回来；**工作记忆**是当前任务的中间状态（已完成哪几步、拿到什么结果），通常放在 State 对象里。常见坑是把所有历史都塞进 prompt，成本随轮次平方增长且触发 Lost in the Middle。",
  },
  {
    id: "q57",
    cat: "Agent",
    q: "MCP 解决什么问题？和 Function Calling 什么关系？",
    a: "Function Calling 是**模型 API 层**的协议——模型返回「调哪个函数、什么参数」。MCP（Model Context Protocol）是**工具提供方层**的协议：把工具、资源、提示词标准化成一个 server，任何支持 MCP 的客户端都能直接接上。类比就是 Function Calling 定义了「怎么调」，MCP 定义了「工具怎么对外暴露」，解决的是 N 个客户端 × M 个工具的重复适配问题。两者不冲突，MCP server 的工具最终还是通过 Function Calling 被调用。",
  },
  {
    id: "q58",
    cat: "Agent",
    q: "Agent 执行到一半失败了怎么办？",
    a: "分三类处理：**工具报错**——把错误信息当结果回传，模型往往能自己换参数重试；**模型输出不合法**（JSON 解析失败）——容错解析 + 重试一次 + 最终降级返回纯文本；**整个任务卡住**（重复调用同一工具、步数耗尽）——中断并把已完成的部分结果返回，别假装成功。关键原则是**每一步的中间状态要可持久化**，否则长任务失败就得从头再来。",
  },
  {
    id: "q59",
    cat: "Agent",
    q: "怎么让 Agent 的行为可控、可审计？",
    a: "四件事：**工具白名单**（模型只能调声明过的，且危险操作单独标记）、**敏感操作人工确认**（删数据、发消息、花钱这类必须卡一道）、**全链路日志**（每一步的思考、调用、结果都记，带同一个 request id）、**防护栏**（最大步数、token 上限、单工具超时、重复调用检测）。这题的加分点是主动提**提示注入**：外部内容里可能藏「忽略之前指令」，所以工具执行权必须在你手里，不能让模型的输出直接变成动作。",
  },
  {
    id: "q60",
    cat: "Agent",
    q: "什么时候该用 workflow 而不是 Agent？",
    a: "流程固定、步骤明确的用 **workflow**（写死的有向图），可预测、可测试、便宜；只有在**步骤数和路径无法提前确定**时才用 Agent 让模型自己决策。Anthropic 那篇 Building Effective Agents 的核心观点就是这个：**大多数场景 workflow 就够了，别为了用 Agent 而用 Agent**。能说出「我先判断能不能用固定流程解决」，比会背 ReAct 更能证明工程判断力。",
  },

  // ---- 评测（原本只有 2 道） ----
  {
    id: "q61",
    cat: "评测",
    q: "黄金集怎么建？多少条够用？",
    a: "**30~100 条起步**，关键不是数量而是覆盖真实分布：从线上真实请求里采样（不是自己编）、覆盖各类任务和难度、必须包含**陷阱题**（问知识库里没有的东西，看它是否老实说不知道）。每条要有期望答案或判分标准。建完先**人工标 20 条验证判分器可靠**，再自动化——否则你自动化的是一个错误的评分标准。之后每次线上发现的 bad case 都补进去，黄金集是活的。",
  },
  {
    id: "q62",
    cat: "评测",
    q: "怎么判断一次 prompt 改动是真的变好了？",
    a: "**必须有基线对比，不能凭感觉**。流程是：改动前在黄金集上跑一遍存基线分数 → 改动后再跑 → 对比每一项的分数变化，不只看总分（总分持平可能是一半变好一半变坏）。开放题用 LLM-as-Judge 且要**交换顺序跑两遍**消除位置偏差。分数下降超过阈值就不许合并——这就是质量门禁。补一句：**样本量小的时候要注意波动**，5 条题差 1 条就是 20%，别过度解读。",
  },
  {
    id: "q63",
    cat: "评测",
    q: "线上没有标准答案，怎么持续监控质量？",
    a: "离线有黄金集，线上靠**代理指标**：用户是否重新提问（一次没答好）、是否点了踩、会话是否异常中断、是否触发了兜底回复、检索命中率、引用校验失败率。再加**抽样人工复核**（每天抽 N 条看）。关键是把这些指标做成趋势看板——**单点数值没意义，突变才是信号**。发现的 bad case 回流到黄金集，形成闭环。",
  },

  // ---- 安全（原本只有 2 道） ----
  {
    id: "q64",
    cat: "安全",
    q: "用户能骗出你的 system prompt 吗？怎么防？",
    a: "**假设一定能被套出来**，所以 system prompt 里不能放密钥、内部 URL、真实业务规则细节这类泄露就有损失的东西。防护是分层的：prompt 里明示不要透露、输出侧做关键词过滤、真正敏感的逻辑放到服务端代码里而不是 prompt 里。这题的正确姿势是**承认防不住，然后说「所以我不依赖它保密」**——比声称「我加了防护所以套不出来」可信得多。",
  },
  {
    id: "q65",
    cat: "安全",
    q: "让模型生成的 SQL 或代码直接执行，有什么风险？",
    a: "风险等同于「把用户输入直接拼进 SQL」，因为**模型输出是不可信输入**。防护：SQL 走只读账号 + 白名单表 + 强制 LIMIT + 禁止 DDL/DML，最好用参数化查询而不是拼字符串；代码执行必须在沙箱里（容器、无网络、超时、资源限额）。更根本的做法是**别让模型自由生成，让它填模板参数**——把生成的自由度收窄到可枚举的范围。",
  },
  {
    id: "q66",
    cat: "安全",
    q: "调用第三方模型 API，数据合规上要注意什么？",
    a: "三件事：**数据出境**（用户数据发到境外 API 可能违规，要确认服务商的部署地域和合规资质）、**训练使用**（确认服务商是否会用你的数据训练，企业版通常可以关闭）、**日志留存**（你自己的日志里如果记了完整 prompt，就等于把用户数据又存了一份，要脱敏或设置保留期）。工程上的应对是在网关层做**PII 脱敏**再转发，以及敏感场景走私有部署。",
  },

  // ---- 工程 ----
  {
    id: "q67",
    cat: "工程",
    q: "怎么降低 LLM 应用的成本？按性价比排序说。",
    a: "从高到低：**① 路由分级**——简单任务给小模型，只有难的才上贵模型，这是最大的一块；**② prompt caching**——固定前缀（system + 工具定义）放最前面且不变，命中就打折；**③ 语义缓存**——重复问题直接返回，命中率高的场景效果显著；**④ 裁剪输入**——历史摘要、检索 top-k 别开太大、去掉冗余的 few-shot；**⑤ 限制输出**——max_tokens 收紧、要求简洁作答（输出通常比输入贵 4~5 倍）。前提是**先按维度归因，知道钱花在哪**再动手。",
  },
  {
    id: "q68",
    cat: "工程",
    q: "多个用户并发请求，你的服务会有什么问题？",
    a: "LLM 请求是**长耗时 IO**（几秒到几十秒），同步阻塞模型下一个请求就把线程占满了。所以要用 asyncio 或多 worker。其次是**上游配额是共享的**，并发一高就集体 429，需要在网关侧做全局限流和排队。还有**流式响应占用连接时间长**，连接数会成为瓶颈。我的网关是单进程同步实现，够本地用，真上线第一件事就是改异步——这个边界我很清楚。",
  },
  {
    id: "q69",
    cat: "工程",
    q: "prompt 怎么做版本管理？",
    a: "**当代码管**：进 git、带版本号、改动走 code review、每个版本关联一次评测运行结果。不要硬编码在业务代码里散落各处，集中放模板文件或配置，业务代码只传变量。上线时**新旧版本可以并行**（小流量灰度对比指标），出问题直接回滚版本号。这题背后考的是「你有没有把 prompt 当成会出 bug 的资产」——很多人还在聊天框里改完直接复制粘贴。",
  },
  {
    id: "q70",
    cat: "工程",
    q: "上游返回的 JSON 格式偶尔不对，怎么保证下游稳定？",
    a: "三层防护。**采样层**：能用 JSON mode 或约束解码（vLLM 的 guided decoding）就用，从根上保证合法。**解析层**：容错处理——剥掉 markdown 代码块围栏、定位第一个 ~{~ 到最后一个 ~}~、失败重试一次并把报错信息给模型让它自己修。**校验层**：用 schema（pydantic / jsonschema）验字段和类型，不符合就走降级路径。关键是**永远不要假设模型会守约**，且失败要有明确的降级行为而不是抛异常给用户。",
  },
  {
    id: "q71",
    cat: "工程",
    q: "同一个 prompt 在不同模型上效果差很多，怎么处理？",
    a: "这是常态，因为各家的对齐方式和指令遵循强度不同。做法是：**prompt 按模型分版本**（配置里给每个模型挂自己的模板），换模型时必须重跑评测而不是直接切。常见差异点：有的模型对 system 更敏感、有的必须把关键约束重复写在 user 里、有的 JSON 输出要显式给例子才稳、有的对中文指令不如英文。我踩过的具体坑是**有中转站会把 system 拼进 user**，所以关键约束我会两处都写一遍。",
  },
  {
    id: "q72",
    cat: "工程",
    q: "如果让你从零设计一个 LLM 应用，第一步做什么？",
    a: "**先定义「什么叫做对了」，也就是先建评测集**。很多人第一步是选框架、选向量库，结果做完了不知道好不好。我的顺序是：明确任务和成功标准 → 建 30 条黄金集 → 用最简单的方案（直接调 API + 手写 prompt）跑基线 → 看差在哪再决定加 RAG / 微调 / Agent。**能不加的不加**——每加一层都是成本和故障点。这题考的是工程节制，答「上 LangChain 搭个 RAG」就落俗了。",
  },
);
